/* 详情 */
.post-content{
    .postad-container{
        overflow: hidden;
    }
}

/* 列表 */
.post-list{
    .post-item{
        .title {
            flex: 1;
        }
        .cover{
            width: 20px;
            height: 20px;
            margin-left: 10px;
            background-size: cover;
            background-repeat: no-repeat;
        }
    }
}
